<div class="center jumbotron">
  <h3> Διάλεξε ομάδα! </h3>

  <div>

    <!-- H lista me tous paiktes ths ghpedouxou omadas -->
    <h3> Hometeam </h3>
     <ul style="list-style-type: none; display: inline-block;">
     		<% @game.hometeam.users.each do |player| %>
       	  <li> <%= player.name %></li>
        <% end %>
        <%= link_to 'Παίξε σε αυτήν την ομάδα' , join_path(@game.hometeam.id)%>
     </ul>


    <!-- H lista me tous paiktes ths filoksenoumenhs omadas -->
     <h3> Awayteam </h3>
     <ul style="list-style-type: none; display: inline-block;">
     	<% @game.awayteam.users.each do |player| %>
       	 <li> <%= player.name %></li>
      <% end %>
        <%= link_to 'Παίξε σε αυτήν την ομάδα' , join_path(@game.awayteam.id) %>
     </ul>
      
  <div>

    <div class='btn '><%= link_to 'Επιστροφή' , games_url %></div>


</div>
